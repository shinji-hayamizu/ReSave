# Injection-Brain 画面遷移図

## 全体フロー

```mermaid
flowchart TD
    subgraph 認証["認証フロー"]
        A[スプラッシュ画面] --> B{ログイン状態?}
        B -->|未ログイン| C[ログイン画面]
        B -->|ログイン済| H[ホーム]
        C --> D[新規登録画面]
        D --> C
        C -->|認証成功| H
        D -->|登録成功| H
        C --> E[パスワードリセット]
        E --> C
    end

    subgraph メイン["メイン機能"]
        H[ホーム] --> H1[予定タブ]
        H --> H2[復習中タブ]
        H --> H3[完了タブ]

        H1 -->|復習開始| N[復習画面]
        H2 -->|続きから| N
        H3 -->|再復習| N

        %% 衝動チェックイン（復讐衝動・反芻）
        H --> U[衝動チェックイン]
        U -->|クールダウン実行| U2[クールダウン]
        U2 -->|終了| U3[再計測/メモ]
        U3 -->|保存| H
        U -->|対処カード化| J
        U -->|履歴| U4[チェックイン履歴]
        U4 -->|詳細| U5[チェックイン詳細]

        H --> I[メモ一覧]
        H --> J[メモ追加]
        H --> K[設定]

        I --> L[メモ詳細]
        L --> M[メモ編集]
        M --> L
        L --> I

        J -->|保存| H

        N -->|完了| H3
        N -->|中断| H2
        N -->|次へ| N
    end

    subgraph 設定["設定"]
        K --> O[プロフィール編集]
        K --> P[通知設定]
        K --> Q[カテゴリ管理]
        K --> R[ログアウト]
        R --> C
        O --> K
        P --> K
        Q --> K
    end

    subgraph 外部["外部連携"]
        S[プッシュ通知] -.->|タップ| H1
        T[Chrome拡張] -.->|クリップ| J
    end
```

## 衝動チェックイン フロー詳細（MVP）

```mermaid
flowchart TD
    UC1[チェックイン開始] --> UC2[強度入力\n衝動0-10/反芻0-10]
    UC2 --> UC3[トリガー/状況メモ（任意）]
    UC3 --> UC4{いま危険？\n(制御不能/強度10など)}
    UC4 -->|はい| UC5[安全導線\n支援先案内/落ち着く行動]
    UC4 -->|いいえ| UC6[クールダウン選択]
    UC6 -->|10分遅延| UC7[タイマー]
    UC6 -->|呼吸/グラウンディング| UC8[短いガイド]
    UC7 --> UC9[再計測\n衝動0-10/反芻0-10]
    UC8 --> UC9
    UC9 --> UC10[保存]
    UC10 --> UC11{対処カードを作る？}
    UC11 -->|はい| UC12[メモ追加へ\nテンプレで作成]
    UC11 -->|いいえ| UC13[ホームへ]
```

## ホーム画面タブ構成

```mermaid
flowchart LR
    subgraph ホーム["ホーム画面"]
        direction TB
        TAB[タブバー] --> T1[予定]
        TAB --> T2[復習中]
        TAB --> T3[完了]

        T1 --> T1C[今日復習予定のカード一覧\n・期限切れ優先表示\n・カテゴリ別表示]
        T2 --> T2C[復習途中のセッション\n・進捗バー表示\n・続きから再開]
        T3 --> T3C[今日完了したカード\n・正答率表示\n・再復習ボタン]
    end
```

## 認証フロー詳細

```mermaid
flowchart LR
    subgraph ログイン
        L1[ログイン画面] --> L2{認証方法}
        L2 -->|メール| L3[メール入力]
        L2 -->|Google| L4[Google OAuth]
        L2 -->|Apple| L5[Apple OAuth]
        L3 --> L6[パスワード入力]
        L6 --> L7{認証結果}
        L4 --> L7
        L5 --> L7
        L7 -->|成功| L8[ホームへ]
        L7 -->|失敗| L9[エラー表示]
        L9 --> L1
    end
```

## 復習フロー詳細

```mermaid
flowchart TD
    R1[復習開始] --> R2[カード表示\n質問面]
    R2 -->|タップ| R3[カード表示\n回答面]
    R3 --> R4{記憶度を選択}
    R4 -->|完璧に覚えた| R5[次回: 7日後]
    R4 -->|だいたい覚えた| R6[次回: 3日後]
    R4 -->|うろ覚え| R7[次回: 1日後]
    R4 -->|忘れた| R8[次回: 数時間後]
    R5 --> R9{残りカード?}
    R6 --> R9
    R7 --> R9
    R8 --> R9
    R9 -->|あり| R2
    R9 -->|なし| R10[復習完了画面]
    R10 --> R11[ホームへ]
```

## 画面一覧

| # | 画面名 | 説明 | 主要機能 |
|---|--------|------|----------|
| 1 | スプラッシュ | 起動時ローディング | 認証状態チェック |
| 2 | ログイン | ログイン画面 | メール/ソーシャルログイン |
| 3 | 新規登録 | アカウント作成 | メール登録/ソーシャル連携 |
| 4 | パスワードリセット | パスワード再設定 | メール送信 |
| 5 | ホーム | メイン画面（3タブ） | 予定/復習中/完了の管理 |
| 5-1 | ├ 予定タブ | 今日の復習予定 | 期限切れ優先、復習開始 |
| 5-2 | ├ 復習中タブ | 進行中セッション | 続きから再開 |
| 5-3 | └ 完了タブ | 今日完了分 | 正答率、再復習 |
| 6 | メモ一覧 | 全メモ表示 | 検索、フィルタ、ソート |
| 7 | メモ追加 | 新規メモ作成 | タイトル、内容、カテゴリ、タグ |
| 8 | メモ詳細 | メモ閲覧 | 内容表示、次回復習日、編集へ |
| 9 | メモ編集 | メモ更新 | 内容編集、削除 |
| 10 | 復習画面 | カード形式復習 | フラッシュカード、記憶度選択 |
| 11 | 設定 | 設定ハブ | 各設定画面へのナビ |
| 12 | プロフィール編集 | ユーザー情報 | 名前、アバター、メール変更 |
| 13 | 通知設定 | リマインド設定 | 通知時間、頻度、ON/OFF |
| 14 | カテゴリ管理 | カテゴリCRUD | 追加、編集、削除、並び替え |
| 15 | 衝動チェックイン | 衝動/反芻入力とクールダウン | 強度入力、タイマー、対処カード化 |
| 16 | チェックイン履歴 | 過去ログ一覧 | フィルタ、検索（任意） |
| 17 | チェックイン詳細 | 翌日レビュー/振り返り | 予測vs実感、メモ化（任意） |

## 状態遷移（メモ）

```mermaid
stateDiagram-v2
    [*] --> 新規作成
    新規作成 --> 学習中: 保存
    学習中 --> 復習待ち: 復習完了
    復習待ち --> 学習中: 復習日到来
    学習中 --> 習得済み: 一定回数正解
    習得済み --> 学習中: 忘れた
    学習中 --> アーカイブ: アーカイブ
    復習待ち --> アーカイブ: アーカイブ
    習得済み --> アーカイブ: アーカイブ
    アーカイブ --> 学習中: 復元
```

## 状態遷移（復習セッション）

```mermaid
stateDiagram-v2
    [*] --> 予定: 復習日到来
    予定 --> 復習中: 復習開始
    復習中 --> 復習中: 次のカード
    復習中 --> 予定: 中断
    復習中 --> 完了: 全カード終了
    完了 --> 復習中: 再復習
    完了 --> [*]: 日付変更
```
